# 任务界面原型结果

每次执行新增一个 `YYYY-MM-DD-<device>-<short-name>.md`，至少记录：

- 结论：`pass`、`fail` 或 `inconclusive`
- Git commit、浏览器/桌面壳版本、分辨率、缩放和硬件摘要
- 使用的数据 profile 与操作路径
- 200 项首屏、滚动、搜索和切换选择的测量方式与结果
- 面包屑、返回、依赖方向、键盘可达性和非颜色状态检查
- 截图、录屏、性能 trace 或自动化报告的仓库相对路径
- 阻塞正式设计的问题，以及是否需要调整 ADR-008
