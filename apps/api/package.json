{
  "name": "@ngapd/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run src --fileParallelism=false",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@fastify/swagger": "^9.6.1",
    "@fastify/swagger-ui": "^5.2.3",
    "@ngapd/contracts": "workspace:*",
    "@ngapd/database": "workspace:*",
    "@ngapd/domain": "workspace:*",
    "@ngapd/object-store": "workspace:*",
    "@ngapd/workspace-core": "workspace:*",
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "@ngapd/test-fixtures": "workspace:*",
    "@ngapd/workspace-cli": "workspace:*"
  }
}
