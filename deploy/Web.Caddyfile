{
	admin off
	auto_https off
}

:8080 {
	encode zstd gzip

	handle /health/live {
		respond "ok" 200
	}

	handle /health/ready {
		respond "ok" 200
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
