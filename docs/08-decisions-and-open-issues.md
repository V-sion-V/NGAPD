# 决策记录与待定事项

文档状态：设计基线 0.5

本文件记录当前文档采用的产品决策。未决事项按表中最晚阶段确认。核心技术 ADR 和 MVP 轻量非功能基线已分别记录在[技术架构决策](09-technical-architecture-decisions.md)和[MVP 非功能基线](10-mvp-non-functional-baseline.md)中。

## 1. 已确认决策

| ID | 决策 | 状态 |
|---|---|---|
| D-001 | 产品采用 Linux 自托管 Web 服务端，并提供 macOS、Windows 本地工作区客户端 | 已确认 |
| D-002 | 项目主要面向少于 20 人的独立游戏团队 | 已确认 |
| D-003 | 同一父任务下预计少于 200 个直接子任务，但不设软件硬限制 | 已确认 |
| D-004 | 工作区以文本为主，偶尔包含图片 | 已确认 |
| D-005 | 一个任务最多一个 Task Owner，只有该 Owner 可以写入任务级工作区，其他项目成员只读 | 已由 D-066 细化为显式 Owner 与单一有效 Owner |
| D-006 | 三种工作区均使用独占写入租约，同一工作区同时最多一个可写连接 | 已确认 |
| D-007 | 权限分为 Web/任务、工作区、Agent 三层，后层不能扩大前层权限 | 已确认 |
| D-008 | Project Admin 必须主动开启管理员模式，才能修改任意任务依赖、Owner、删除/归档等跨成员结构 | 已由 D-053 细化归档/删除层级 |
| D-009 | 管理员模式不能绕过任务级工作区 Owner 单写权限；Project Owner/Admin 的项目级写权限不要求开启管理员模式 | 已由 D-055 细化 Agent 例外 |
| D-010 | 普通成员只能修改自己拥有的任务；依赖修改采用“拥有任一端点或共同直接父任务”的独立例外规则 | 已被 D-061 取代 |
| D-011 | 父任务 Owner 不自动拥有已经指派给他人的后代节点 | 内容和工作区隔离继续保留；后代 Owner 分配权由 D-066 取代旧限制 |
| D-012 | 普通成员可以把自己的任务指派给别人；转交完成后失去该任务写权限 | 已被 D-066 的继承式即时指派规则取代 |
| D-013 | 不开启管理员模式时，修改依赖要求当前用户拥有 predecessor、successor 或两者共同直接父任务中的至少一个；顶层任务以虚拟项目根为共同父级并由 Project Owner 控制；其他情况必须进入管理员模式 | 已被 D-061 取代 |
| D-014 | Agent 先通过 Web 与工作区权限，再执行自身确认策略 | 已确认 |
| D-015 | Agent 可以无确认读取全部授权任务信息，但对任何任务管理数据修改都必须人工确认 | 已确认 |
| D-016 | Agent 对当前用户有写资格且持有租约的任一类型工作区具有完整访问权限，可直接创建、修改、移动、重命名和删除文件；仍受路径、租约和审计约束 | 已被 D-055 细化 |
| D-017 | Agent 不成为 Task Owner，只代表发起用户操作 | 采用建议默认值 |
| D-018 | 工作区与游戏源码仓库分离，首版只保存引用，不替代 Git | 采用建议默认值 |
| D-019 | 工作区文件同步到服务端，并保存版本化快照 | 采用建议默认值 |
| D-020 | 任务只维护截止时间，不维护预计工时 | 已由 D-058 明确为 UTC 时间点 |
| D-021 | Project Key 为 2—6 个大写字母，全局唯一且不可修改 | 采用建议默认值 |
| D-022 | Task Key 单调分配且不复用 | 采用建议默认值 |
| D-023 | 子任务全部完成只是父任务完成的必要条件；父任务仍需显式完成 | 已由 D-053 细化为启用态子任务 |
| D-024 | “已阻塞”是依赖和人工 blocker 共同计算出的有效状态 | 采用建议默认值 |
| D-025 | 验收工作使用普通任务节点表达，相关目标、说明和验收信息统一写入任务正文，不新增复杂审批流 | 采用建议默认值 |
| D-026 | 允许未分配任务，但开始执行前必须有 Owner | 已被 D-066 取代：显式 Owner 可空，但通过祖先继承有效 Owner |
| D-027 | 移动任务会使依赖跨父时拒绝移动，不自动删除或迁移依赖 | 采用建议默认值 |
| D-028 | 任务默认归档而非硬删除，Task Key 不释放 | 已被 D-053 取代 |
| D-029 | 已完成任务可以重新打开，并创建新工作周期、保留历史摘要 | 采用建议默认值 |
| D-030 | AI 完成摘要先作为草稿，允许 Owner 修订和确认 | 已由 D-060 明确为独立 KnowledgeEntry |
| D-031 | 系统模板复制到项目后独立演进，不自动覆盖 | 采用建议默认值 |
| D-032 | Agent 工具优先采用 MCP 类标准协议，并保持模型供应商无关 | 采用建议默认值 |
| D-033 | 增加任务评论和必要通知，但评论不赋予工作区操作权 | 已确认 |
| D-034 | 首版以中文界面和中文预设为主，数据层预留多语言 | 采用建议默认值 |
| D-035 | 首版不提供自定义字段和自定义状态机 | 采用建议默认值 |
| D-036 | 首版只预留 Git 外部链接，不做 Git 平台深度集成 | 采用建议默认值 |
| D-037 | 任务的目标、描述和验收条件合并为一个统一正文 `content` 字段 | 已确认 |
| D-038 | 顶层任务统一视为虚拟项目根节点的直接子任务，因此顶层任务之间可以建立依赖 | 已确认 |
| D-039 | 父任务满足全部完成条件时通知 Task Owner“可完成”，但不自动完成 | 已确认 |
| D-040 | 任务可标记为普通、冲刺或里程碑；该标记只改变渲染外观，不影响业务逻辑 | 已确认 |
| D-041 | 初版不实现语义缩放，使用本级任务列表、当前任务详情、面包屑、子任务列表和返回上一级按钮 | 已确认 |
| D-042 | Agent 只能在用户显式确认后进入管理员模式，不能自动进入；可以在不再需要时自动退出 | 已确认 |
| D-043 | Agent 连接任务后读取用户级个人流程/Skill、项目级通用规则/Skill、当前用户项目自我介绍、全部逻辑角色描述和当前任务完整信息 | 已由 D-060 补充活动祖先当前信息 |
| D-044 | 工作区分为用户级、项目级、任务级；每个用户、项目、任务分别拥有一个对应工作区 | 已确认 |
| D-045 | 用户级只有对应用户可写，其他已认证用户只读；项目级只有 Project Owner/Admin 可写，其他项目成员只读；任务级只有 Task Owner 可写，其他项目成员只读 | 继续保留；任务级写入者由 D-066 明确为有效 Owner |
| D-046 | 用户级用于个人流程、自用规则和 Skill；项目级用于项目通用规则、资料和 Skill；任务级用于过程文档和产物 | 已确认 |
| D-047 | Project Admin 写项目级工作区属于工作区权限，不需要进入 Web 任务管理员模式 | 已由 D-055 限定为人直接写入；Agent 写入例外 |
| D-048 | 一个 Agent 会话默认只以一个工作区为当前可写目标，其他相关工作区作为只读上下文来源 | 设计具体化 |
| D-049 | 项目级规则优先于用户级个人流程；只自动加载当前用户自用 Skill，不自动加载其他用户 Skill | 设计具体化 |
| D-050 | 其他已认证用户继续拥有用户级工作区底层只读权限；Agent 不得自行发现、搜索或读取，只有用户明确指定目标和目的后才能读取 | 用户明确确认 |
| D-051 | `.ngapd/` 是本地控制面，不进入用户内容同步；`TASK.md`、`SUMMARY.md` 是服务端只读投影，Agent 不得写入 | 用户明确确认与一致性修正 |
| D-052 | Workspace 服务端生命周期、本地副本状态和连接/租约状态分别建模，不把物化和连接状态作为 Workspace 全局状态 | 用户采纳建议 |
| D-053 | 只有顶层任务可归档且不提供任务恢复；非顶层任务只能不可恢复删除；顶层归档依赖保留为无约束历史；父任务完成只检查启用态子任务 | 用户明确确认 |
| D-054 | 成员被移除时，其拥有任务的 Owner 统一置空；Project Owner 必须先完成由接收者接受的所有权转移 | Task 部分已被 D-066 取代；Project Owner 接受式转移保留 |
| D-055 | Agent 可直接操作本人用户级和本人任务级用户内容；写项目级工作区必须由用户明确要求并确认进入管理员模式，人直接写项目级工作区仍不要求管理员模式 | 用户明确确认 |
| D-056 | 每个父级依赖作用域维护 `graph_version`；提案、接受和确认绑定图版本与影响集合指纹 | 用户采纳建议 |
| D-057 | 租约失效或服务端版本不匹配后，本地改动不作数；下次连接直接由服务端权威版本替换，不合并、不恢复、不重新导入 | 已被 D-068 取代 |
| D-058 | 截止时间统一存储和传输为 UTC 时间点，前端按设备系统时区显示 | 用户明确确认 |
| D-059 | Task 增加一个可选 `logical_role_id`，用于表达主要能力要求并支持过滤，但不授予权限 | 用户明确确认 |
| D-060 | 完成摘要作为独立、版本化 KnowledgeEntry 管理；`SUMMARY.md` 只是只读投影；子任务上下文包含活动祖先当前信息 | 用户采纳建议 |
| D-061 | 跨 Owner 依赖不能由单一端点 Owner 单方面生效：拥有两端或共同父任务可直接修改，只拥有一个端点时由另一端点 Owner 接受；顶层由 Project Owner 控制 | 用户采用建议 |
| D-062 | 任何跨成员 Task Owner 指派都必须由目标成员接受，包括新建、未分配任务指派和转移；自我指派与成员移除导致 Owner 置空除外 | 已被 D-066 取代 |
| D-063 | M7 是 MVP 后额外需求，不属于 M0—M6 发布质量门槛 | 已由 D-069 调整：项目级备份恢复提前到 MVP，M7 仍为额外增强 |
| D-064 | 本地 Agent IPC、OS ACL、宿主配对、短期本地能力和令牌存储在详细设计阶段确定，最晚在 M5 开始前冻结 | 用户明确延期 |
| D-065 | 技术栈 ADR 和量化非功能基线另立文档，不在本轮设计基线中展开 | 用户明确延期 |
| D-066 | Task 保存可空显式 Owner；显式 Owner 为空时继承最近的显式 Owner 非空祖先。任一任务的有效 Owner 可即时为后代指派显式 Owner，无需接收者接受；非顶层显式 Owner 可主动清空并回落给最近祖先，顶层显式 Owner 不能为空 | 用户明确确认 |
| D-067 | 任务完成时把继承出的有效 Owner 固化为显式 Owner；随后冻结字段、Owner、结构、依赖、阻塞和任务级工作区，仅允许评论、显式重新打开和顶层归档；项目通过 deny/cascade 设置处理重新打开 predecessor 时的已完成 successor | 用户明确确认 |
| D-068 | 租约失效或版本冲突时不静默覆盖；仍有写资格的用户重新取得租约后明确选择本地或服务端版本成为新的唯一事实，Agent 不得代选 | 用户明确确认 |
| D-069 | MVP 提供项目级一致备份与原项目恢复；产品不提供项目永久删除、任务回收站或单任务恢复。非顶层任务删除要求输入完整 Task Key，日常整理优先完成并归档顶层任务 | 用户明确确认 |
| D-070 | Task 可以单向关注同项目另一 Task；Agent 连接关注方后可以无需额外确认地渐进读取被关注任务中当前用户本来有权读取的数据。关注只展开一跳，不授予写权限，也不参与依赖或完成计算 | 用户明确提出 |
| D-071 | `Project.owner_membership_id` 是 Project Owner 唯一权威来源；Membership 的 `permission_level` 只有 admin/member，不再保存 owner，也不提供 Viewer | 用户明确确认 |
| D-072 | 任务移动按稳定顺序锁定源、目标父级图版本，并与依赖增删共享事务边界；移动成功同时递增两个作用域的 `graph_version` | 用户采纳审查建议 |
| D-073 | 项目级 Skill 的来源、脚本和宿主工具风险由小团队自行治理；MVP 不增加签名、发布审批或额外 Skill 能力系统，底层工具仍执行既有权限校验 | 用户明确接受风险 |
| D-074 | 将最小 Workspace 服务端契约、完成快照、冻结和 Owner/租约原子协调提前到 M2；M4 负责跨平台客户端与真实同步，M6 不再首次引入完成快照 | 用户采纳审查建议 |
| D-075 | 父任务 Owner 对已经由他人有效拥有的直接子任务不增加截止时间、正文、状态或层级修改权；除后代显式 Owner 分配权和既有依赖作用域规则外，跨 Owner 修改仍需管理员模式 | 用户采用 O-001 默认值 |
| D-076 | MVP 管理员模式在 30 分钟无操作后失效；默认不强制每次重新认证，高风险部署可以配置重新认证 | 用户采用 O-004 默认值 |
| D-077 | MVP 通知只提供站内渠道；邮件、聊天软件等外部通知渠道排到后续版本 | 用户采用 O-008 默认值 |
| D-078 | 评论正文使用 Markdown 文本；附件不直接存入评论记录，只引用当前用户有权读取的工作区文件 | 用户采用 O-009 默认值 |
| D-079 | 初期实际部署按少于 10 名用户、小规模项目、内网或 VPN 访问的一台 Linux 服务器设计；使用 Docker Compose，优先保证交付，不为高并发、高可用集群或大规模容量提前增加基础设施 | 用户明确确认 |
| D-080 | NGAPD 服务端不调用外部 API 或 LLM，全部业务数据自托管。通过 Agent 完成任务时，由用户自己的 Agent/模型生成摘要并随完成提案提交；人工完成且未提供摘要时先提示，用户仍继续则由服务端使用任务名称、正文摘要、状态和完成时间生成确定性的一句话基本摘要 | 用户明确确认 |
| D-081 | MVP 采用 TypeScript 单仓库、Fastify 模块化单体、PostgreSQL、React Web、Electron 桌面客户端、REST/OpenAPI、SSE、PostgreSQL 后台作业和本地内容寻址文件存储；单机 Docker Compose 部署，不引入 Redis、独立搜索服务、MinIO 或 Kubernetes | 已被 D-083 的客户端部分取代；其余服务端与部署选型继续有效 |
| D-082 | 首批逻辑角色模板采用策划、技术、美术、音乐与声音四个角色族；“技术”同时包含程序开发和 QA。等级暂用 L1/L2/L3，分别表示指导下执行、独立负责专业模块、负责跨模块标准与最终决策；角色不要求拥有完整等级序列，正式清单见 `11-logical-role-templates.json` | 用户审阅后确认 |
| D-083 | 当前客户端基础改为无界面的 Workspace CLI：通过 MCP stdio 向 Agent 提供受控能力，人工只使用状态与诊断；UI 无关能力放入共享核心，未来同步平台适配器或 GUI 复用该核心，不恢复 Electron 空壳 | Workspace CLI 替代功能，详见 09 文档 ADR-014 |

## 2. 权限解释基线

任务责任使用“显式 Owner + 最近祖先继承”的单一有效 Owner 模型：

- Task 自身显式 Owner 非空时由该成员有效拥有；为空时由最近一个显式 Owner 非空祖先的 Owner 有效拥有。
- 任一任务的有效 Owner 可以为任意后代即时设置显式 Owner，因此顶层任务 Owner 对整个子树拥有 Owner 分配权；目标成员不需要接受。
- 子任务一旦设置其他显式 Owner，祖先 Owner 仍可调整其 Owner 字段，但不能编辑其内容、改状态或写其任务级工作区。
- 非顶层显式 Owner 可以主动清空自己的显式 Owner，使责任回落给最近祖先；顶层显式 Owner 不能为空。
- 涉及他人任务的子树移动、顶层归档、非顶层删除等结构操作需要管理员模式。
- 依赖增删采用双端责任规则：拥有两端或共同直接父任务可以直接修改；只拥有一个端点时由另一端点 Owner 接受；顶层由 Project Owner 控制。
- 管理员模式仍然不能写他人的任务级工作区；项目级工作区由 Project Owner/Admin 根据独立写入策略和租约访问。

这能保持每个任务始终最多一个有效责任人，同时允许顶层到后代的明确分工。

## 3. Agent 确认解释基线

当前文档把 Agent 操作划分为：

- 无需确认：读取全部授权任务管理信息；读取默认分层工作区、项目角色和知识；在当前用户有写资格且已获得租约的用户级或任务级工作区中创建、修改、移动、重命名和删除用户内容；正常同步；退出管理员模式。
- 需要明确用户意图：读取其他用户的用户级工作区；Agent 不得自行发现、搜索或加载。
- 需要显式管理员模式：Agent 写入项目级工作区；必须由用户先明确要求本次项目维护并确认进入。
- 需要确认：任何任务管理数据修改，包括标题、统一正文、UTC 截止时间、逻辑角色、标签、展示类型、评论、任务创建/拆分、状态、依赖、显式 Owner、关注、层级、完成和重新打开。
- 需要明确影响预览和确认：进入管理员模式、跨 Owner 修改、级联重新打开、任务归档、批量修改、强制租约接管、工作区冲突取舍、非顶层不可恢复删除和项目级恢复。

人工确认不能替代权限；无权操作即使确认也应拒绝。

## 4. 已延期或待对应阶段决定的开放事项

| ID | 需要决定的问题 | 当前默认 | 最晚决定阶段 |
|---|---|---|---|
| O-001 | 父任务 Owner 是否应对他人拥有的直接子任务具有某些结构权，例如改截止日期？ | 已由 D-075 决定：没有；必须管理员模式 | 已解决 |
| O-003 | 被指派成员是否必须接受 Task Owner 转移？ | 已由 D-066 决定：不需要；指派即时生效 | 已解决 |
| O-004 | 管理员模式会话持续多久，是否要求重新认证？ | 已由 D-076 决定：30 分钟无操作失效；高风险部署可要求重新认证 | 已解决 |
| O-005 | 截止日期是纯日期还是精确到时间及时区？ | 已由 D-058 决定：UTC 时间点，前端按系统时区显示 | 已解决 |
| O-006 | 单文件和工作区的默认软限制是多少？ | 作为部署配置，不写入领域硬限制 | M4 |
| O-007 | 租约 TTL、断网宽限期和强制接管等待时间 | 通过同步原型测量后确定 | M4 |
| O-008 | 通知首版仅站内，还是同时支持邮件？ | 已由 D-077 决定：首版仅站内；外部渠道后续 | 已解决 |
| O-009 | 评论是否允许附件和 Markdown？ | 已由 D-078 决定：Markdown 文本；附件作为工作区引用 | 已解决 |
| O-010 | 项目永久删除后 Project Key 是否永不释放？ | 已由 D-069 取消：产品不提供项目永久删除 | 已解决 |
| O-011 | 完成摘要由哪类模型生成，是否允许完全关闭 AI 摘要？ | 已由 D-080 决定：服务端不调用模型；用户自己的 Agent 可生成，缺失时使用确定性基本摘要 | 已解决 |
| O-012 | 系统首批预设逻辑角色的名称、层级和能力文本 | 已由 D-082 和 `11-logical-role-templates.json` 确定 | 已解决 |
| O-013 | 项目名称 NGAPD 的正式全称和开源许可证 | 暂不阻塞架构 | M7 前 |
| O-014 | 服务端、数据库、客户端、文件监听、本地 IPC、对象存储和 API 契约的技术 ADR | 已建立 09 技术架构决策；本地 IPC 安全细节仍按 D-064 在 M5 前复核冻结 | 已解决核心选型 |
| O-015 | P95 延迟、总任务规模、递归操作预算、同步吞吐和恢复时间等量化基线 | 已建立 10 MVP 轻量非功能基线；按交付优先原则只保留单机小规模必需门槛 | 已解决 MVP 基线 |

## 5. 需要特别验证的假设

### H-001：三级工作区边界足够稳定

假设：用户级、项目级、任务级分层，加上按 Task Key 平铺的任务目录，能让 Agent 保持上下文隔离，同时不会给成员带来过高的目录管理成本。

验证：让真实任务连续使用多个工作周期，观察跨任务引用、搜索和人工整理成本。

### H-002：摘要能替代大量原始过程读取

假设：用户级个人流程、项目级通用规则、活动祖先当前信息，以及 ancestor/predecessor 的历史确认摘要足以为 Agent 提供必要背景，不需要加载所有工作区完整原文。

验证：比较只用摘要与加载原文时的任务理解和完成质量。

### H-003：同级依赖限制符合实际工作

假设：跨父任务依赖可以通过在共同父任务下提升/增加协调节点表达，不需要任意跨树边。

验证：记录真实项目中无法自然表达的依赖案例；在放宽限制前优先改进建模指引。

### H-004：三层权限足够简单

假设：管理员模式、三级工作区独占写入和 Agent 确认模型既能防误操作，又不会给小团队造成明显负担。

验证：统计管理员模式使用频率、被拒绝但用户认为合理的操作，以及 Agent 确认接受/拒绝率。

### H-005：平铺树状导航足以支撑初版

假设：本级任务列表、当前任务详情、面包屑、子任务列表和返回上一级按钮，足以清晰表达初版的任务分解与同级依赖。

验证：让用户使用平铺树状界面完成深层任务定位、顶层/同级依赖分析和进度理解；记录必须依赖语义缩放才能解决的问题，作为后续版本输入。

## 6. 变更规则

- 已确认决策发生变化时，新增一条决策记录，不直接删除旧结论。
- 领域不变量变化必须同步更新产品需求、领域模型、权限模型、Agent 工具和测试计划。
- 权限放宽必须给出具体威胁分析和回归测试。
- 工作区格式通过 `schemaVersion` 演进，客户端必须能够识别不支持的新版本并安全降级为只读。
